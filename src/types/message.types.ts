export interface MessageTypes {
    _id: string;
    text?: string;
    timestamp: Date;
    senderId: string;
    receiverId: string;
    message: string;
    createdAt: string;
    updatedAt: string;
}
